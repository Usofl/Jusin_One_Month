// 220331.cpp : 콘솔 응용 프로그램에 대한 진입점을 정의합니다.
//

#include "stdafx.h"

int main()
{
	/* 
	   그러게 한번더 구글 봐야겠다
	   흔히 얘기하는 코드몽키 (원숭이처럼 따라친다고_)
	   !!제네릭
	   템플릿, 인라인, 연산자 오버로딩
	   인라인 함수는 그게 가능함
	   그래서 매크로 c++에서는 잘안씀
	   defalt 대입연산자가 연산자 오버로딩!

	   string + string 이 연산자 오버로딩!

	   조건 자료형,
	   
	   교환 법칙 해결 시 전역 함수로 만들어야함..

	   if(iTemp > iSour)
	   {
	   return true
	   }
	   return false

	   return (iTemp > iSour);
	*/
	
	// string 클래스 만들기

    return 0;
}

